# icon forge

A VS Code extension to generate icons for your projects. It provides a simple interface to create icons from text, images, or SVG files, and allows you to customize the output format and size.
